#include <bits/stdc++.h>
#include <iostream>
using namespace std;
int main()
{

    deque<int> d;
    d.push_back(12);
    cout << "front element: " << d.front() << endl;
    d.push_front(13);
    cout << "front element: " << d.front() << endl;
    d.push_front(14);
    cout << "front element: " << d.front() << endl;
    d.push_back(15);
    int len = d.size();
    for (int i = 0; i < len; i++)
    {
        cout << d[i] << " ";
    }
    cout << "front element: " << d.front() << endl;
    d.pop_front();
    cout << "front element: " << d.front() << endl;
    d.pop_back();
    cout << "Back element: " << d.back() << endl;
    dfjbfjbdf
        kfbdwfnew
            qknfjf
                kansfkqsf

        return 0;
}